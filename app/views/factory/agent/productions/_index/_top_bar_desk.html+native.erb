<div class="as-bar">
  <i class="px-5"></i>
  <div class="bar-title"><%= @desk.name %></div>
  <%= button_to({ controller: '/space/desks', action: 'change', con: 'factory/agent/productions', station_id: @desk.room.station_id }, form: { class: 'px-4' }) do %>
    <%= svg_tag 'ellipsis-vertical' %>
  <% end %>
</div>
