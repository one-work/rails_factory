<div class="as-bar">
  <i class="left"></i>
  <div class="bar-title"><%= @desk.name %></div>
  <%=
    button_to(
      { controller: '/space/desks', action: 'change', con: 'factory/agent/productions' },
      params: { desk_id: @desk.id },
      form: { class: 'right' }
    ) do
  %>
    <%= svg_tag 'ellipsis-vertical' %>
  <% end %>
</div>
