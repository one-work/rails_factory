<%= render layout: 'top_bar_left' do %>
  <div class="bar-title"><%= @contact.name %></div>
  <%= button_to({ action: 'preview' }, params: params.permit(:contact_id), class: 'text-link', form: { class: 'right' }) do %>
    <span>选择客户</span>
    <%= svg_tag 'angle-right' %>
  <% end %>
<% end %>
