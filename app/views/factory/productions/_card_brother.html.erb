<%= button_to({ action: 'dialog', id: brother.id }, method: :patch, class: brother.id == @production.id ? 'button is-primary' : 'button') do %>
  <span><%= brother.name %></span>
  <span class="pl-2"><%= brother.price.to_money.format.html_safe %></span>
<% end %>
