<%= form_with url: { action: 'batch_receive' }, class: 'button_to' do |f| %>
  <%= f.hidden_field :ids %>
  <%= button_tag disabled: true, class: 'button is-success is-rounded' do %>
    <i class="fa-solid fa-cash-register"></i>
    <span class="ml-1"><%= t('.batch_receive.title') %></span>
  <% end %>
<% end %>
