<%=
  tr_tag(
    model,
    {
      href: url_for(controller: 'fits', product_id: model.id),
      icon: 'gears',
      title: t('.fits.index.title')
    },
    data: {
      url: url_for(action: 'reorder', id: model.id),
      id: model.id
    },
    icons: ['edit']
  ) { yield }
%>
